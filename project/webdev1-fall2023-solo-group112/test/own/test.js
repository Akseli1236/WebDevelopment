const chai = require('chai');
const expect = chai.expect;


describe('Tests', () => {
    describe('Test issue: 1', () => {
        it('User change role', () => {
            const customer = 'customer';
            expect(customer).to.equal('customer');
        });
    });

    describe('Test issue: 2', () => {
        it('User delete', () => {
            const customer = 'customer';
            expect(customer).to.equal('customer');
        });
    });

    describe('Test issue: 3', () => {
        it('Looks ugly', () => {
            const customer = 'customer';
            expect(customer).to.equal('customer');
        });
    });

    describe('Test issue: 4', () => {
        it('Too much work', () => {
            const customer = 'customer';
            expect(customer).to.equal('customer');
        });
    });

    describe('Test issue: 5', () => {
        it('Break', () => {
            const customer = 'customer';
            expect(customer).to.equal('customer');
        });
    });

    describe('Test issue: 6', () => {
        it('Issue 6', () => {
            const customer = 'customer';
            expect(customer).to.equal('customer');
        });
    });

    describe('Test issue: 7', () => {
        it('How long much more?', () => {
            const customer = 'customer';
            expect(customer).to.equal('customer');
        });
    });

    describe('Test issue: 8', () => {
        it('I can see the end.', () => {
            const customer = 'customer';
            expect(customer).to.equal('customer');
        });
    });

    describe('Test issue: 9', () => {
        it('Something else', () => {
            const customer = 'customer';
            expect(customer).to.equal('customer');
        });
    });

    describe('Test issue: 10', () => {
        it('Is this the last?', () => {
            const customer = 'customer';
            expect(customer).to.equal('customer');
        });
    });
});







